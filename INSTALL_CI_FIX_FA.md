# اصلاح یکپارچگی سورس Raha Life 0.8.0

این بسته سورس کامل نسخه 0.8.0 موجود در بسته تحویلی قبلی را، همراه با اصلاحات CI، در ریشه ZIP قرار می‌دهد. خود مخزن فعلی گیت‌هاب دریافت نشده است.

## نصب
1. ZIP را استخراج کنید؛ خود ZIP را به عنوان سورس در گیت‌هاب آپلود نکنید.
2. تمام محتویات آن، از جمله .github، lib، test، tool، assets و pubspec.yaml و pubspec.lock را در ریشه مخزن جایگزین کنید. pubspec.yaml باید کنار README.md باشد، نه داخل یک پوشه Raha-Life دیگر.
3. برای انتقال کامل، GitHub Desktop مناسب است: مخزن را Clone کنید، محتویات استخراج‌شده را روی پوشه مخزن کپی و Replace کنید، سپس Commit و Push کنید. پوشه .git مخزن را حذف نکنید.
4. در Actions اجرای Build Raha Life را باز کنید. پس از موفقیت هر job، خروجی Android، Windows یا Web در Artifacts قرار می‌گیرد.

عنوان Commit:
fix: restore complete release source and guard CI builds

## تغییرات
- فایل‌های workspace، synced_feature_refresh و sync_controller سازگار با boot و همگام‌سازی خودکار در بسته هستند.
- main.dart از فونت داخلی استفاده می‌کند و import قدیمی google_fonts ندارد.
- کنترل 170 فایل انتشار پیش از اجرای Flutter، آپلود ناقص و مخلوط‌شدن نسخه‌ها را تشخیص می‌دهد.
- PowerShell پس از خطای تحلیل از ادامه تست و ساخت جلوگیری می‌کند.
- APK عمومی بلافاصله پس از ساخت کپی می‌شود تا ساخت‌های بعدی آن را از بین نبرند.

## اعتبارسنجی این نوبت
کنترل importهای نسبی Dart، تجزیه YAML چهار workflow، تطبیق فایل‌های انتشار و آزمون تشخیص فایل حذف‌شده موفق بود. Flutter SDK و PHP در این محیط در دسترس نیستند؛ تحلیل Flutter، تست‌های Flutter و ساخت APK/Windows در این نوبت اجرا نشده‌اند. نتیجه 50 تست در گزارش قدیمی، نتیجه این نوبت نیست. پذیرش نهایی وابسته به اجرای GitHub Actions است.

برای تغییر عمدی سورس در آینده، پس از بازبینی تغییرات دستور زیر را اجرا و manifest را هم commit کنید:
python tool/check_source_integrity.py --refresh
این دستور را برای پنهان‌کردن آپلود ناقص اجرا نکنید.
