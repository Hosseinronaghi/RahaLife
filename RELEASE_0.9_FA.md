# مدیریت زندگی رها — نسخهٔ ۰٫۹ برای بررسی

مبنای این تغییرات همان `Raha-Life-v0.8.0-CI-Fix` تأییدشده است. شمارهٔ نسخه `0.9.0+16` شده است. این بسته **پایان همهٔ درخواست‌ها یا تأیید انتشار عمومی نیست**. قابلیت‌های آزمایشی و کارهای باقی‌مانده در پایین مشخص‌اند.

## تغییرات انجام‌شده

| خواسته | نتیجهٔ این بسته | حدود اعتبارسنجی |
|---|---|---|
| نام پس از نصب | نام نمایشی Raha Life در تنظیمات تولیدشدهٔ پلتفرم‌ها | اسکریپت برندینگ و حفظ شناسه‌ها آزموده شد؛ نصب دستگاه انجام نشده |
| عنوان فارسی | مدیریت زندگی رها در ترجمهٔ فارسی و عنوان پنجرهٔ دسکتاپ | رابط Flutter و اسکریپت پلتفرم |
| آیکون جدید | طرح جدید و اندازه‌های Android، Apple، Windows و وب | فایل‌های نهایی و اتصال به گردش ساخت؛ بستهٔ بومی روی دستگاه بررسی نشده |
| نصب روی نسخهٔ قبلی | شناسه‌ها و مسیرهای داده حفظ؛ شمارهٔ ساخت افزایش یافته؛ ابزار مقایسهٔ APK اضافه | **بدون گواهی/کلید نسخهٔ قبلی تضمین نصب ممکن نیست** |
| یادداشت | عنوان و اطلاعات جانبی فاصله‌گذاری شده، بخش اطلاعات جانبی باز و بسته می‌شود؛ حفظ متن غنی و موعد | رابط در سه عرض، متن بزرگ، ذخیرهٔ دوباره بدون ایجاد نسخهٔ تکراری |
| یادداشت خراب | در صورت خوانده‌نشدن سند، ذخیره مسدود می‌شود تا محتوای قبلی بازنویسی نشود | تحلیل کد؛ مسیر پیام خطا نیازمند نمونهٔ واقعی خراب نیز هست |
| بستن فرم‌ها | کنترلرها پس از پایان عمر پنجره آزاد می‌شوند | باز و بسته‌کردن فرم افراد، مالی و چرخه در تست رابط |
| امور مالی | افزودن و ویرایش نوع حساب، بانک، شماره حساب، واحد پول، بایگانی؛ ویرایش تراکنش؛ تسویهٔ بدهی/طلب؛ سررسید آن‌ها؛ دسترسی به گزارش | انتقال، ویرایش انتقال، حساب بایگانی و ارز ناسازگار آزموده شده |
| گزارش مالی | ستون ارز CSV اصلاح شده؛ حساب Cash در صفحه و فرم مالی پول نقد نمایش داده می‌شود | تحلیل و ساخت؛ تمام گزارش‌های مالی حسابرسی مستقل نشده‌اند |
| بودجه و اقساط | امکانات نسخهٔ پایه حفظ شده‌اند | توسعهٔ کاملاً جدید حسابداری دوبل/چندارزی با نرخ تبدیل ادعا نمی‌شود |
| افراد | نسبت‌های آماده و گزینهٔ دلخواه؛ ویرایش اطلاعات؛ تاریخ تولد با مبنای شمسی/میلادی و ورود تاریخ شمسی | تست مدل، رابط و سازگاری دادهٔ قدیمی |
| اتصال تولد | تولد از اطلاعات فرد خوانده می‌شود؛ تغییر نام/تاریخ و پاک‌کردن تاریخ منعکس می‌شود؛ کپی‌های قدیمی دوباره نمایش داده نمی‌شوند | تست ویرایش، حذف تاریخ، تکرار شمسی و رکوردهای قدیمی |
| چرخه | ویرایش سوابق، اعتبارسنجی شروع/پایان، برآورد فاصله و مدت از سوابق اخیر، تعداد نمونه و هشدار قدیمی‌شدن برآورد | تست مدل و رابط |
| هم‌زمانی چرخه | نمایش برنامه‌های همان بازه؛ یادآور خصوصی اختیاری یک روز قبل | منطق تقویم؛ دریافت واقعی اعلان روی گوشی تأیید نشده |
| تداخل پزشکی | این قابلیت فقط هم‌زمانی تقویمی است | تشخیص تداخل دارویی یا توصیهٔ درمانی پیاده نشده و از آن ادعایی نمی‌شود |
| انتخاب بخش‌های سینک | انتخاب هنگام ساخت اتصال و صفحهٔ تغییر انتخاب برای اتصال موجود؛ صف سایر بخش‌ها ارسال نمی‌شود؛ دریافت صفحه‌ای فیلتر می‌شود | تست صف با بیش از صد رکورد خارج از انتخاب و تست HTTP واقعی |
| حریم خصوصی انتخاب سینک | اتصال جدید، مالی/سلامت/افراد/رویدادها را پیش‌فرض انتخاب نمی‌کند؛ اتصال قدیمی تنظیم قبلی‌اش را حفظ می‌کند | برای بازبینی اتصال قدیمی وارد «انتخاب بخش‌ها» شوید |
| هاست اشتراکی | سرور PHP/MySQL موجود توسعه یافته؛ شناسهٔ تغییر و پیام در محدودهٔ صاحب حساب است | PHP 8.3 و MariaDB 10.11 موقت؛ هاست واقعی شما بررسی نشده |
| مجوز و اشتراک | خواندن/ویرایش، کنترل نسخه، لغو و مسدودکردن دوستی در سرور حفظ و آزموده شد | اشتراک فعلی یک کپی مستقل است، نه ویرایش زندهٔ همان سند محلی |
| پیام آفلاین PHP | صف پایدار، شناسهٔ ثابت برای تلاش مجدد، تفکیک سرور و حساب، نمایش و لغو پیام معلق، کش گفتگو | تست صف و جلوگیری از ثبت پیام تکراری در سرور |
| Matrix | اتصال اختیاری با توکن، فهرست اتاق‌ها، عضویت/ساخت گروه، خواندن پیام، ارسال با شناسهٔ ثابت | API با پاسخ‌های آزمون؛ اتصال واقعی چند سرور آزمایش نشده |
| اتاق رمزگذاری‌شدهٔ Matrix | ارسال متن ساده به آن مسدود است؛ پیوند بازکردن در Element وجود دارد | **E2EE در خود رها، تأیید دستگاه و بازیابی کلید پیاده نشده‌اند** |
| Mastodon | اتصال اختیاری با توکن، خط زمانی، انتشار با پیش‌نمایش و انتخاب مخاطب، پسندیدن | درخواست‌های HTTP با پاسخ‌های آزمون؛ انتشار واقعی انجام نشده |
| حضور هم‌زمان دو شبکه | تنظیمات مستقل Matrix/Mastodon؛ هیچ داده‌ای از ماژول‌ها خودکار منتشر نمی‌شود | پل خودکار میان دو شبکه پیاده نشده |
| GitHub و خروجی | برندینگ در گردش ساخت اجرا می‌شود؛ کنترل پوشهٔ اشتباه `lib/app/app`؛ پک کامل و فایل‌های تغییرکرده | ساخت وب محلی؛ اجرای گردش GitHub از این محیط انجام نشده |

## کارهای باقی‌مانده برای پایان تمام درخواست‌ها

1. رمزنگاری سرتاسری Matrix در خود برنامه، مدیریت/تأیید دستگاه‌ها و بازیابی کلید؛ اتصال فعلی از نوع پیش‌نمایش بدون E2EE است. کتابخانهٔ Matrix فعلی از SQLite 3.x استفاده می‌کند، درحالی‌که نسخهٔ پایه SQLite 2.9.4 دارد؛ مهاجرت این وابستگی و آزمون رمزنگاری هنوز انجام نشده است.
2. اشتراک و همکاری زندهٔ یادداشت، پروژه و خرید از طریق Matrix، حل تعارض در همکاری گروهی و اعلان تغییرات بین سرورهای مستقل.
3. فدراسیون مستقیم بین سرورهای PHP رها و پیاده‌سازی ActivityPub در خود سرور رها. اتصال به حساب Mastodon به معنی ساخت سرور ActivityPub نیست.
4. پل انتخابی Matrix/Mastodon و قابلیت‌های کامل دنبال‌کردن، پاسخ و مدیریت شبکه. API اتصال فعلی تمام امکانات Element یا Mastodon را بازتولید نمی‌کند.
5. یکدست‌سازی و بازبینی بصری همهٔ صفحات قدیمی، ترجمهٔ تمام پیام‌های خام خطای سرور/کتابخانه، بررسی کامل فرم‌های تخصصی و پیوست‌های اشتراکی. قسمت‌های تغییرکرده بررسی شده‌اند؛ کل برنامه به‌صورت دستی پیمایش نشده است.
6. آزمون Android/Windows/macOS/iOS/Linux روی خروجی بومی، اعلان در پس‌زمینه و دستگاه واقعی؛ این بسته شامل APK امضاشدهٔ نهایی یا نصب‌کنندهٔ دسکتاپ نیست.
7. تأیید ارتقای واقعی با APK قبلی و گواهی امضای همان نسخه؛ هیچ کلید امضای جدیدی به‌جای کلید قبلی ساخته نشده است.
8. استقرار و آزمون سرتاسری روی هاست، دامنه و حساب‌های آزمایشی شما. موفقیت تست محلی، تأیید عملیاتی آن محیط نیست.

## نکات عملی

- خاموش‌کردن یک بخش در سینک، نسخهٔ قبلاً ارسال‌شده را از سرور حذف نمی‌کند. بکاپ کامل و رمزگذاری‌شده مستقل از انتخاب بخش‌های سینک است.
- پیام‌ها و سینک PHP و پیام‌رسانی آزمایشی Matrix در این نسخه E2EE نیستند. HTTPS برای اتصال بیرونی لازم است.
- دعوت/لغو مجوز، نسخهٔ ذخیره‌شده توسط گیرنده را از دستگاه او پس نمی‌گیرد.
- بازهٔ چرخه تقریبی است؛ هم‌زمانی با قرار یا کار، به معنی منع انجام آن کار نیست.
- برای تغییر سرور/حساب، صف پیام‌ها به همان سرور و حساب اولیه مقید است.
- گزارش تست نهایی در `validation/` قرار دارد. تست‌های رابط شامل عرض‌های ۳۶۰، ۸۰۰ و ۱۲۸۰ و مقیاس متن ۱٫۴ هستند.
